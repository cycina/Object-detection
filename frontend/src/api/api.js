import { http } from './config'

export const loginUser = http + 'login'
export const logOut = http + 'logout'
export const getProduct = http + 'product'
export const getOrder = http + 'order'
export const getResults = http + 'result'
export const getUser = http + 'user'
export const register = http + 'register'
